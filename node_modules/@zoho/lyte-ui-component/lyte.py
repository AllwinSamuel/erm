import json
import sys
 
input_files = sys.argv[1]
f = open(input_files)
data = json.load(f)
 
dep_list=["lyte-dom","lyte-cli","lyte-ui-component","lyte","lyte-editor" ,"lyte-shape","crux-components","lytmus","convert-to-asyncawait","toAsyncAndAwait"]
dep_pkg=''
dep_pkg_zoho=''
if 'dependencies' in data:
    for dep in data['dependencies']:
        if dep in dep_list:
            dep_pkg = dep_pkg + dep + ","
            dep_pkg_zoho = dep_pkg_zoho + "@zoho/" + dep + ","
 
if dep_pkg:
    print str(dep_pkg) + "*"  + str(dep_pkg_zoho)
 
else:
    print "package not available"

f.close()
