#!/bin/sh

# Script to remove CNTRL M characters.

if [ $# = 0 ]; then
	echo "Usage: $0 file [file ...] "
	exit 1
else
    for FILE in $@
    do
	perl -e 's/\r//g' -w -p -i $FILE
    	echo -e "CNTRL M characters removed in $FILE.\n"
    done
fi
