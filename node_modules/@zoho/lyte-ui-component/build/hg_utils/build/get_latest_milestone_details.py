#! /usr/bin/python

from clonehook import *
import sys

classObject = updateTime("GET")
if len(sys.argv) > 2:
	latest_milestone_details = classObject.getLatestMilestone(sys.argv[1], sys.argv[2]);
	if latest_milestone_details:
		print latest_milestone_details
	else:
		print None
else:
	print "python get_latest_milestone_details.py <BRANCH NAME> <PRODUCT_NAME>"
	sys.exit(1)

