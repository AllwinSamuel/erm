#! /usr/bin/python
from clonehook import *
import sys

if len(sys.argv) > 1:
    milestonelabel = sys.argv[1];
    classObject = updateTime("GET")
    previousMilestoneDetails = classObject.getPreviousMilestoneChangeset(milestonelabel);
    if previousMilestoneDetails:
        ml_src_changeset = previousMilestoneDetails[0];
        ml_tp_changeset = previousMilestoneDetails[1];
	ml_build_id = previousMilestoneDetails[2];
    else:
        ml_src_changeset = "";
        ml_tp_changeset = "";
	ml_build_id = "";
    print str(ml_src_changeset)+"$"+str(ml_tp_changeset)+"$"+str(ml_build_id)
else:
    print "python api_previous_milestone_changeset.py <milestonelabel>"
    sys.exit(0)

